[package]
name = "weaver-internal"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "The operator's promotion space: loop-reachable pure callables, dispatched inward"

# One library target and nothing else, per weaver-internal-Spec section 1: a
# member holds no process and binds no socket, so there is no binary here to
# declare. No normal dependency, on purpose - the manifest form of the charter's
# pure bar and the operator's ruling of 2026-09-23 on #577 that nothing is
# compiled in unless operations require it. Build and dev dependencies are
# admitted. tests/manifest.rs is the instrument that reads it.

[dependencies]
